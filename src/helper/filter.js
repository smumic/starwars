const filter = (items, filterOptions) => {

  function filter(obj) {
    if (true) {
      return true;
    } else {
      return false;
    }
  }
  return items.filter(filter);
};

export default filter;
